const initialState = {
  remember_token: null
};

function profile(state, action) {
  if(state == null) return initialState;
  switch(action.type) {
    default:
      return state;    
  }

}

export default profile;
